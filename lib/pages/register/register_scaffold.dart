import 'package:flutter/material.dart';

class RegisterQRScaffold extends StatelessWidget {
  const RegisterQRScaffold();
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register QR Code'),
      ),
      body: Column(
        children: [
          Text('Sample test'),
        ],
      ),
    );
  }
}
